<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Splash screen application theme.
    <style name="Theme.ToursDeliberations.AppSplash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@drawable/splash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_tours_couleur</item>
        <item name="postSplashScreenTheme">@style/Theme.ToursDeliberations</item>
    </style>-->

    <!-- Base application theme. -->
    <style name="Theme.ToursDeliberations" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorOnSurface">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
    </style>
</resources>



